<UserControl x:Class="RikkaTracker.Views.FilterSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:RikkaTracker.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:FilterSettingsViewModel}">
    <!-- @Author: trae + deepseek-V4-pro -->
    <!-- @Date: 2026-06-23 -->
    <!-- @Desc: Segoe Fluent Icons 字体资源，用于 FontIcon 标记扩展 -->
    <UserControl.Resources>
        <FontFamily x:Key="SegoeFluentIcons">Segoe Fluent Icons</FontFamily>
    </UserControl.Resources>
    <Grid Margin="20">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <ui:TextBlock Text="{DynamicResource StrFilterRules}" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>
                
                <ui:Card Padding="10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <ItemsControl ItemsSource="{Binding FilterRules}" 
                                      BorderThickness="0" 
                                      Background="Transparent" 
                                      Margin="0,0,0,10">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <ui:TextBox Text="{Binding ProcessPattern, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="{DynamicResource StrProcessPatternPlaceholder}" Margin="0,0,10,0"/>
                                        
                                        <CheckBox Grid.Column="1" Content="{DynamicResource StrIgnoreRecord}" IsChecked="{Binding Ignore}" VerticalAlignment="Center"/>
                                        <CheckBox Grid.Column="2" Content="{DynamicResource StrDisableIdle}" IsChecked="{Binding DisableIdleDetection}" VerticalAlignment="Center"/>
                                        
                                        <!-- @Desc: Delete → \uE74D (Segoe Fluent Icons) -->
                                        <ui:Button Grid.Column="3" 
                                                   Icon="{ui:FontIcon FontFamily={StaticResource SegoeFluentIcons}, Glyph=&#xE74D;}" 
                                                   Appearance="Transparent" 
                                                   Command="{Binding DataContext.RemoveRuleCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
                                                   CommandParameter="{Binding}"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        
                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
                            <!-- @Desc: Add → \uE710 (Segoe Fluent Icons) -->
                            <ui:Button Content="{DynamicResource StrAddRule}" Icon="{ui:FontIcon FontFamily={StaticResource SegoeFluentIcons}, Glyph=&#xE710;}" Command="{Binding AddRuleCommand}"/>
                            <ui:Button Content="{DynamicResource StrSaveAndApply}" Appearance="Primary" Margin="10,0,0,0" Command="{Binding SaveRulesCommand}"/>
                            <ui:Button Content="{DynamicResource StrResetToDefaults}" Appearance="Secondary" Margin="10,0,0,0" Command="{Binding ResetToDefaultsCommand}"/>
                        </StackPanel>
                    </Grid>
                </ui:Card>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
