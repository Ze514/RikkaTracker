<UserControl x:Class="RikkaTracker.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:local="clr-namespace:RikkaTracker.Views"
             xmlns:vm="clr-namespace:RikkaTracker.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             d:DataContext="{d:DesignInstance vm:DashboardViewModel}">
    <Grid Margin="20">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <ui:TextBlock Text="{DynamicResource StrTodayOverview}" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>
                
                <!-- Row 1: 5 cards -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Total Time -->
                    <ui:Card Grid.Column="0" Margin="0,0,8,0" Padding="14">
                        <StackPanel>
                            <ui:TextBlock Text="{DynamicResource StrTotalTime}" FontSize="11" Margin="0,0,0,4" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                            <ui:TextBlock Text="{Binding TotalTimeText}" FontSize="20" FontWeight="Bold"/>
                        </StackPanel>
                    </ui:Card>

                    <!-- Top App -->
                    <ui:Card Grid.Column="1" Margin="8,0,8,0" Padding="14">
                        <StackPanel>
                            <ui:TextBlock Text="{DynamicResource StrTopApp}" FontSize="11" Margin="0,0,0,4" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Image Source="{Binding TopAppIcon}" Width="20" Height="20" Margin="0,0,6,0"/>
                                <ui:TextBlock Text="{Binding TopAppName}" FontSize="14" FontWeight="Bold" TextTrimming="CharacterEllipsis" MaxWidth="80" VerticalAlignment="Center"/>
                                <ui:TextBlock Text="{Binding TopAppTimeText}" FontSize="13" Opacity="0.7" Margin="6,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>
                    </ui:Card>

                    <!-- Top Site -->
                    <ui:Card Grid.Column="2" Margin="8,0,8,0" Padding="14">
                        <StackPanel>
                            <ui:TextBlock Text="{DynamicResource StrTopSite}" FontSize="11" Margin="0,0,0,4" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ui:TextBlock Text="{Binding TopSiteDomain}" FontSize="14" FontWeight="Bold" TextTrimming="CharacterEllipsis" MaxWidth="90" VerticalAlignment="Center"/>
                                <ui:TextBlock Text="{Binding TopSiteTimeText}" FontSize="13" Opacity="0.7" Margin="6,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>
                    </ui:Card>

                    <!-- Most Active Hour -->
                    <ui:Card Grid.Column="3" Margin="8,0,8,0" Padding="14">
                        <StackPanel>
                            <ui:TextBlock Text="{DynamicResource StrMostActiveHour}" FontSize="11" Margin="0,0,0,4" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                            <ui:TextBlock Text="{Binding MostActiveHour}" FontSize="20" FontWeight="Bold"/>
                        </StackPanel>
                    </ui:Card>

                    <!-- Total Web Time -->
                    <ui:Card Grid.Column="4" Margin="8,0,0,0" Padding="14">
                        <StackPanel>
                            <ui:TextBlock Text="{DynamicResource StrWebTotalTime}" FontSize="11" Margin="0,0,0,4" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                            <ui:TextBlock Text="{Binding WebTotalTimeText}" FontSize="20" FontWeight="Bold"/>
                        </StackPanel>
                    </ui:Card>
                </Grid>

                <!-- Row 2: App chart (left) + Web chart (right) -->
                <Grid Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- App 24h chart -->
                    <ui:Card Grid.Column="0" Padding="20">
                        <StackPanel>
                            <ui:TextBlock Text="{DynamicResource StrAppChartTitle}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15"/>
                            <Grid Height="180">
                                <lvc:CartesianChart Series="{Binding Series}" XAxes="{Binding XAxes}" YAxes="{Binding YAxes}" TooltipPosition="Top" Background="Transparent" />
                            </Grid>
                        </StackPanel>
                    </ui:Card>

                    <!-- Web 24h chart -->
                    <ui:Card Grid.Column="2" Padding="20">
                        <StackPanel>
                            <ui:TextBlock Text="{DynamicResource StrWebChartTitle}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15"/>
                            <Grid Height="180">
                                <lvc:CartesianChart Series="{Binding WebSeries}" XAxes="{Binding WebXAxes}" YAxes="{Binding WebYAxes}" TooltipPosition="Top" Background="Transparent" />
                            </Grid>
                        </StackPanel>
                    </ui:Card>
                </Grid>

                <Border Height="20"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
