<UserControl x:Class="RikkaTracker.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:local="clr-namespace:RikkaTracker.Views"
             xmlns:vm="clr-namespace:RikkaTracker.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             d:DataContext="{d:DesignInstance vm:DashboardViewModel}">
    <Grid Margin="20">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <ui:TextBlock Text="{DynamicResource StrTodayOverview}" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- 总时长 -->
                    <ui:Card Grid.Column="0" Margin="0,0,10,0" Padding="20">
                        <StackPanel>
                            <ui:TextBlock Text="{DynamicResource StrTotalTime}" FontSize="12" Margin="0,0,0,5" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                            <ui:TextBlock Text="{Binding TotalTimeText}" FontSize="24" FontWeight="Bold"/>
                        </StackPanel>
                    </ui:Card>

                    <!-- 活跃应用 -->
                    <ui:Card Grid.Column="1" Margin="10,0,10,0" Padding="20">
                        <StackPanel>
                            <ui:TextBlock Text="{DynamicResource StrActiveApps}" FontSize="12" Margin="0,0,0,5" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                            <ui:TextBlock Text="{Binding AppCount}" FontSize="24" FontWeight="Bold"/>
                        </StackPanel>
                    </ui:Card>

                    <!-- 最长使用 -->
                    <ui:Card Grid.Column="2" Margin="10,0,0,0" Padding="20">
                        <StackPanel>
                            <ui:TextBlock Text="{DynamicResource StrTopApp}" FontSize="12" Margin="0,0,0,5" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Image Source="{Binding TopAppIcon}" Width="24" Height="24" Margin="0,0,8,0"/>
                                <ui:TextBlock Text="{Binding TopAppName}" FontSize="18" FontWeight="Bold" TextTrimming="CharacterEllipsis" MaxWidth="120" VerticalAlignment="Center"/>
                                <ui:TextBlock Text="{Binding TopAppTimeText}" FontSize="16" Opacity="0.7" Margin="8,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>
                    </ui:Card>
                </Grid>

                <Grid Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- 较昨日变化 -->
                    <ui:Card Grid.Column="0" Margin="0,0,10,0" Padding="20">
                        <StackPanel>
                            <ui:TextBlock Text="{DynamicResource StrVsYesterday}" FontSize="12" Margin="0,0,0,5" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                            <ui:TextBlock Text="{Binding TimeComparedToYesterday}" FontSize="24" FontWeight="Bold"/>
                        </StackPanel>
                    </ui:Card>

                    <!-- 最活跃时段 -->
                    <ui:Card Grid.Column="1" Margin="10,0,10,0" Padding="20">
                        <StackPanel>
                            <ui:TextBlock Text="{DynamicResource StrMostActiveHour}" FontSize="12" Margin="0,0,0,5" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                            <ui:TextBlock Text="{Binding MostActiveHour}" FontSize="24" FontWeight="Bold"/>
                        </StackPanel>
                    </ui:Card>

                    <!-- 平均单应用时长 -->
                    <ui:Card Grid.Column="2" Margin="10,0,0,0" Padding="20">
                        <StackPanel>
                            <ui:TextBlock Text="{DynamicResource StrAvgTimePerApp}" FontSize="12" Margin="0,0,0,5" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                            <ui:TextBlock Text="{Binding AverageTimePerApp}" FontSize="24" FontWeight="Bold"/>
                        </StackPanel>
                    </ui:Card>
                </Grid>

                <!-- 活跃趋势图 -->
                <ui:Card Margin="0,20,0,0" Padding="25">
                    <StackPanel>
                        <ui:TextBlock Text="{DynamicResource StrHourlyTrend}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,20"/>
                        
                        <Grid Height="200">
                            <lvc:CartesianChart Series="{Binding Series}" XAxes="{Binding XAxes}" YAxes="{Binding YAxes}" TooltipPosition="Top" />
                        </Grid>
                        
                        <!-- 底部间距 -->
                        <Border Height="20"/>
                    </StackPanel>
                </ui:Card>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
