<UserControl x:Class="RikkaTracker.Views.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:RikkaTracker.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:StatisticsViewModel}">
    <Grid Margin="20">
        <StackPanel>
            <ui:TextBlock Text="分析统计" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
            
            <ui:Card Padding="40">
                <StackPanel HorizontalAlignment="Center">
                    <ui:SymbolIcon Symbol="Clock24" FontSize="64" Opacity="0.2"/>
                    <ui:TextBlock Text="历史分析功能开发中..." FontSize="18" Margin="0,20,0,0" Opacity="0.5"/>
                    <ui:TextBlock Text="即将支持饼图与占比雷达图" FontSize="14" HorizontalAlignment="Center" Opacity="0.3"/>
                </StackPanel>
            </ui:Card>
        </StackPanel>
    </Grid>
</UserControl>
