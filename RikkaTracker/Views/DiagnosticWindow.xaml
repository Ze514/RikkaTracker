<ui:FluentWindow x:Class="RikkaTracker.Views.DiagnosticWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="{DynamicResource StrDiagnosticConsole}" Height="500" Width="700"
        Icon="pack://application:,,,/Assets/app-icon.ico"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        Background="Transparent"
        Topmost="True">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- TitleBar -->
        <ui:TitleBar Grid.Row="0">
            <ui:TitleBar.Header>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                    <Image Source="pack://application:,,,/Assets/app-icon.png" Width="33" Height="33" VerticalAlignment="Center" />
                    <TextBlock Text="{DynamicResource StrDiagnosticConsole}" VerticalAlignment="Center" Margin="8,0,0,0" Foreground="{DynamicResource TextFillColorPrimaryBrush}" FontSize="14" />
                </StackPanel>
            </ui:TitleBar.Header>
        </ui:TitleBar>

        <!-- Toolbar -->
        <Border Grid.Row="1" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <!-- Filter TextBox -->
                <ui:TextBox x:Name="TxtFilter" Grid.Column="0" 
                            PlaceholderText="{DynamicResource StrProcessFilter}"
                            TextChanged="TxtFilter_TextChanged"
                            Margin="0,0,8,0"
                            VerticalAlignment="Center"/>

                <!-- Action Buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <ui:Button x:Name="BtnTopmost" 
                               Icon="{ui:SymbolIcon Pin24}" 
                               Content="{DynamicResource StrTopmost}"
                               Click="BtnTopmost_Click"
                               Appearance="Secondary"
                               Margin="0,0,8,0"/>
                    <ui:Button x:Name="BtnClear" 
                               Icon="{ui:SymbolIcon Delete24}" 
                               Content="{DynamicResource StrClearLogs}"
                               Click="BtnClear_Click"
                               Appearance="Secondary"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Logs ListBox -->
        <ListBox x:Name="LstLogs" Grid.Row="2" Background="Transparent" BorderThickness="0" Padding="8" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                    <Setter Property="Padding" Value="4"/>
                    <Setter Property="Margin" Value="0,2"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border CornerRadius="4" Padding="8,6" Background="{DynamicResource ControlAltFillColorQuaternaryBrush}" BorderThickness="3,0,0,0" BorderBrush="{Binding StatusBrush}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="180" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            
                            <!-- Timestamp -->
                            <TextBlock Grid.Column="0" Text="{Binding TimestampString}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontFamily="Consolas" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            
                            <!-- Process Name & PID -->
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,12,0">
                                <TextBlock Text="{Binding ProcessName}" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" FontFamily="Segoe UI Semibold" TextTrimming="CharacterEllipsis" MaxWidth="120" ToolTip="{Binding ProcessName}"/>
                                <TextBlock Text="{Binding ProcessIdString}" Foreground="{DynamicResource TextFillColorTertiaryBrush}" FontFamily="Consolas" Margin="4,0,0,0" FontSize="11" VerticalAlignment="Center"/>
                            </StackPanel>

                            <!-- Transition Info (Old -> New Status) -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,16,0">
                                <TextBlock Text="{Binding OldStatusString}" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                                <ui:SymbolIcon Symbol="ArrowRight24" Margin="6,0" FontSize="12" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                                <TextBlock Text="{Binding NewStatusString}" Foreground="{Binding StatusBrush}" FontWeight="Bold"/>
                            </StackPanel>

                            <!-- Window Title -->
                            <TextBlock Grid.Column="3" Text="{Binding WindowTitle}" TextTrimming="CharacterEllipsis" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" ToolTip="{Binding WindowTitle}"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</ui:FluentWindow>
