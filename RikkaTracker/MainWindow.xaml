<ui:FluentWindow x:Class="RikkaTracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:RikkaTracker"
        xmlns:vm="clr-namespace:RikkaTracker.ViewModels"
        xmlns:views="clr-namespace:RikkaTracker.Views"
        mc:Ignorable="d"
        Title="RikkaTracker" Height="750" Width="1200"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        Background="Transparent">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <views:DashboardView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <views:SettingsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ActivityListViewModel}">
            <views:ActivityListView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:StatisticsViewModel}">
            <views:StatisticsView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- TitleBar -->
        <ui:TitleBar
            Title="RikkaTracker"
            Grid.Row="0">
            <ui:TitleBar.Header>
                <TextBlock Text="{Binding StatusText}" Foreground="{StaticResource BrushPink}" VerticalAlignment="Center" Margin="10,0,0,0"/>
            </ui:TitleBar.Header>
        </ui:TitleBar>

        <ui:NavigationView
            Grid.Row="1"
            PaneDisplayMode="Left"
            IsBackButtonVisible="Collapsed"
            IsPaneToggleVisible="True"
            OpenPaneLength="200">
            
            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem
                    Content="概览"
                    Command="{Binding NavigateToDashboardCommand}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="Home24"/>
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
                
                <ui:NavigationViewItem
                    Content="应用统计"
                    Command="{Binding NavigateToActivityListCommand}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="Apps24"/>
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>

                <ui:NavigationViewItem
                    Content="分析统计"
                    Command="{Binding NavigateToStatisticsCommand}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="ChartMultiple24"/>
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
            </ui:NavigationView.MenuItems>

            <ui:NavigationView.FooterMenuItems>
                <ui:NavigationViewItem
                    Content="设置"
                    Command="{Binding NavigateToSettingsCommand}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="Settings24"/>
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
            </ui:NavigationView.FooterMenuItems>
        </ui:NavigationView>

        <ContentPresenter Grid.Row="1" Margin="210,10,10,10" Content="{Binding CurrentViewModel}" />
    </Grid>
</ui:FluentWindow>
