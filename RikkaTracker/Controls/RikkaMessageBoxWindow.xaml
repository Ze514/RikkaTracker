<ui:FluentWindow x:Class="RikkaTracker.Controls.RikkaMessageBoxWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="提示" 
        Width="450"
        SizeToContent="Height"
        MinWidth="400"
        MaxWidth="600"
        MinHeight="180"
        MaxHeight="400"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- TitleBar -->
            <RowDefinition Height="*"/>    <!-- Content -->
            <RowDefinition Height="Auto"/> <!-- Buttons -->
        </Grid.RowDefinitions>

        <!-- TitleBar -->
        <ui:TitleBar Grid.Row="0" Name="TitleBar"
                     ShowMaximize="False"
                     ShowMinimize="False"
                     ShowClose="False"
                     CloseWindowByDoubleClickOnIcon="False"/>

        <!-- Content Area -->
        <Grid Grid.Row="1" Margin="24,12,24,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Status Icon -->
            <ui:SymbolIcon x:Name="StatusIcon" 
                            FontSize="36" 
                            Margin="0,4,16,0" 
                            VerticalAlignment="Top"
                            Grid.Column="0"/>

            <!-- Message Text -->
            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" MaxHeight="220" Focusable="False">
                <ui:TextBlock x:Name="MessageText" 
                              Text="提示内容" 
                              FontSize="14" 
                              TextWrapping="Wrap" 
                              VerticalAlignment="Center"
                              LineHeight="20"/>
            </ScrollViewer>
        </Grid>

        <!-- Buttons Area -->
        <Border Grid.Row="2" Background="{ui:ThemeResource ControlFillColorDefaultBrush}" BorderBrush="{ui:ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,1,0,0">
            <StackPanel x:Name="ButtonsPanel" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Right" 
                        Margin="24,16">
                <!-- 动态创建的按钮将放置在此处 -->
            </StackPanel>
        </Border>
    </Grid>
</ui:FluentWindow>
