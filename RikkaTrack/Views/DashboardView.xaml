<UserControl x:Class="RikkaTrack.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="概览仪表盘" Style="{StaticResource TitleText}" Foreground="White" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding WelcomeMessage}" Style="{StaticResource BaseText}" Foreground="#AAAAAA" Margin="0,10,0,0"/>
            
            <!-- 这里以后会放图表 -->
            <Border Background="{StaticResource BrushPinkShadow}" Height="200" Width="400" CornerRadius="10" Margin="0,20,0,0">
                <TextBlock Text="图表占位符 (LiveCharts2)" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
