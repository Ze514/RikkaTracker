<UserControl x:Class="RikkaTrack.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Padding="40">
            <StackPanel>
            <TextBlock Text="设置" Style="{StaticResource TitleText}" Foreground="White" Margin="0,0,0,30"/>
            
            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock Text="开机自启动" Foreground="White" Width="100" VerticalAlignment="Center"/>
                <CheckBox IsChecked="{Binding IsAutoStart}" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock Text="数据存储路径" Foreground="White" Width="100" VerticalAlignment="Center"/>
                <TextBox Text="{Binding StoragePath}" Width="300" IsReadOnly="True" Background="#333333" Foreground="White" BorderThickness="0" Padding="5"/>
            </StackPanel>
            
            <Button Content="导出数据" Background="{StaticResource BrushOrangeShadow}" Foreground="White" BorderThickness="0" Padding="10,5" Width="100" HorizontalAlignment="Left" Margin="0,20,0,0"/>
        </StackPanel>
        </Border>
    </Grid>
</UserControl>
